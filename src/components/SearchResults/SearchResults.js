// import { useEffect, useState } from "react";

export default function SearchResults({ searchQuery: images }) {
  console.log(images);

  return (
    <div></div>
    // <div className="masonry-grid">
    //   {images &&
    //     images.map((image) => (
    //       <div className="masonry-grid-item">
    //         <img
    //           loading="lazy"
    //           key={image.id}
    //           src={image.urls.regular}
    //           alt={image.alt_description}
    //         />
    //       </div>
    //     ))}
    // </div>
  );
}
